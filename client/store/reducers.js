// import C from '../constants'
import { combineReducers } from 'redux'

// export const bid = (state=10, action) => {
// 	if (action.type === C.ADD_BID) {
// 		return parseInt(action.payload)
// 	} else {
// 		return state
// 	}
// }

export default combineReducers ({
	// bid
})
